task :build do
  `gem build ./ClockworkComicPDF.gemspec`
end

task :test do
  system('clear;rubocop')
  require_relative 'lib/clockworkcomicpdf.rb'
  Dir.chdir('test')
  ClockworkComicPDF.book_from_yaml 'test.yml'
end
